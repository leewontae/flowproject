<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.jhta.mybatis.mapper.GongiMapper">
  
  
  <sql id="search">
  <if test="field!= null and field!=''">
  
  	where ${field} like '%'||#{keyword}||'%'
  	
  </if>
  
  </sql>
  
    <sql id="allsearch">
  <if test="field!= null and field!='' and field.equals('all')">
  
  	where title like '%'||#{keyword}||'%' or content like '%'||#{keyword}||'%'
  	
  </if>
  
  
  
  </sql>
  
  	<delete id="delete" parameterType="int">
	
	delete from gongi where num=#{num}
	
	</delete>
  
<insert id="insert" parameterType="com.jhta.gongi.vo.GongiVo">

	insert into gongi values(gongi_seq.nextval,#{writer},#{title},#{content},sysdate)

</insert>
  

  <select id="list"  parameterType="hashmap" resultType="com.jhta.gongi.vo.GongiVo">
  	
  	select * from(
 select aa.*, rownum rnum from
 (select * from gongi 
 
 <include refid="search"/>
 
 order by num desc)aa) 
 <![CDATA[where rnum>=#{startRow} and rnum<=#{endRow}]]>
  
  </select>
  
  <select id="count" parameterType="hashmap" resultType="int" >
	
		
		 select NVL(count(*),0) cnt from gongi 
		  <include refid="search"/>
	

	</select>
	
	<select id="alllist"  parameterType="hashmap" resultType="com.jhta.gongi.vo.GongiVo">
  	
  	select * from(
 select aa.*, rownum rnum from
 (select * from gongi 
 
 <include refid="allsearch"/>
 
 order by num desc)aa) 
 <![CDATA[where rnum>=#{startRow} and rnum<=#{endRow}]]>
  
  </select>
  
  <select id="allcount" parameterType="hashmap" resultType="int" >
	
		
		 select NVL(count(*),0) cnt from gongi 
		  <include refid="allsearch"/>
	

	</select>
	
	
  </mapper>